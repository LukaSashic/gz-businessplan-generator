# Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!

# ============================================
# NEXT.JS
# ============================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# ============================================
# SUPABASE (https://supabase.com)
# ============================================
# Project Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Project Settings → API → Project API keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Project Settings → API → Project API keys → service_role
# ⚠️  CRITICAL: Only use server-side, NEVER expose to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# ANTHROPIC CLAUDE API (https://console.anthropic.com)
# ============================================
# API Keys → Create Key
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ⚠️  CRITICAL: Zero Data Retention header required for DSGVO
# This is handled automatically in code, no env var needed
# Header: 'anthropic-beta': 'zdr-2024-10-22'

# ============================================
# UPSTASH REDIS (https://upstash.com) - Rate Limiting
# ============================================
# Optional for MVP, required for production
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here

# ============================================
# SENTRY (https://sentry.io) - Error Tracking
# ============================================
# Optional for MVP, recommended for production
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# ============================================
# LANGFUSE (https://langfuse.com) - LLM Observability
# ============================================
# Optional for MVP, recommended for production
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key
LANGFUSE_HOST=https://cloud.langfuse.com

# ============================================
# DEVELOPMENT TOOLS
# ============================================
# Enable verbose logging in development
NEXT_PUBLIC_DEBUG=false

# Skip type checking during dev (faster rebuilds)
# Set to false for production builds
SKIP_TYPE_CHECK=false

# ============================================
# FEATURE FLAGS
# ============================================
# Enable/disable features without code changes
NEXT_PUBLIC_ENABLE_VOICE_COACHING=false
NEXT_PUBLIC_ENABLE_WEB_RESEARCH=true
NEXT_PUBLIC_ENABLE_DOCUMENT_EXPORT=true

# ============================================
# PRODUCTION OVERRIDES
# ============================================
# These are set automatically by Vercel in production
# Only needed if deploying elsewhere

# Vercel automatically sets:
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=your-app.vercel.app
# VERCEL_REGION=fra1

# ============================================
# TESTING
# ============================================
# Used in E2E tests
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=test-password-here

# ============================================
# NOTES
# ============================================
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in actual values (get from service dashboards)
# 3. NEVER commit .env.local to git
# 4. Update .env.example when adding new variables
# 5. Validate env vars with Zod schema (see lib/env.ts)
# 6. Use NEXT_PUBLIC_ prefix ONLY for client-side vars
# 7. Server-side vars (API keys) must NOT have NEXT_PUBLIC_ prefix

# ============================================
# GETTING API KEYS
# ============================================

# Supabase:
# 1. Create project at https://supabase.com
# 2. Choose Frankfurt (fra) region for DSGVO compliance
# 3. Settings → API → Copy URL and anon key

# Anthropic:
# 1. Sign up at https://console.anthropic.com
# 2. API Keys → Create Key
# 3. Add credits (starts at $5)

# Upstash Redis (optional):
# 1. Create account at https://upstash.com
# 2. Create Redis database
# 3. Choose global region
# 4. Copy REST URL and token

# Sentry (optional):
# 1. Create project at https://sentry.io
# 2. Copy DSN from project settings
# 3. Create auth token for releases

# Langfuse (optional):
# 1. Sign up at https://langfuse.com
# 2. Create project
# 3. Settings → API Keys → Copy public and secret keys
